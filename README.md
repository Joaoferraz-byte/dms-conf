# Noctalia configuration for Livara

This repository is the reproducible first-layer integration for **Noctalia v5** in the Livara NixOS environment. It contains a curated Noctalia TOML, a small set of reviewed v5 plugins, and the Home Manager module that installs them as store-backed files.

## Ownership model

| Layer | Owner | Contract |
|---|---|---|
| NixOS hardware, drivers, graphics, PipeWire, portals and keyd | `nix-conf` | System modules and host conditionals |
| Niri compositor, outputs, window rules and native compositor actions | `nix-conf` | Declarative KDL generated by Home Manager |
| Noctalia bar, panels, launcher, notifications, wallpaper, lockscreen, widgets and palette | This repository | `programs.noctalia` and `config/noctalia/config.toml` |
| Noctalia plugin source | This repository | Vendored, reviewed sources under `plugins/*/plugin.toml` |
| Application-specific adapters | `shell-conf`/`nix-conf` | Native application contracts consuming the Noctalia palette |

The Home Manager module enables Noctalia but deliberately disables its systemd unit. Niri starts exactly one shell with `spawn-at-startup "noctalia"`, so the Wayland and D-Bus environment comes from the compositor session and there is no duplicate process.

## Declarative configuration and mutable state

`config/noctalia/config.toml` contains stable intent only. Noctalia may still write runtime settings and plugin data under the user state directory, but generated widget geometry, monitor-specific IDs, caches, last-wallpaper values and materialized plugin catalogs are not part of this repository. A clean installation receives the same source configuration and deterministic plugin sources; runtime state is recreated by Noctalia.

The local Niri template is enabled and writes the optional `~/.config/niri/noctalia.kdl` include used by the declarative `config.kdl` in `nix-conf`. This keeps compositor policy in `nix-conf` while delegating wallpaper-derived focus-ring and border colors to the Noctalia template. Built-in GTK, Qt, Kitty, WezTerm, Starship and KDE templates are enabled because their outputs are separate application theme contracts. The local palette template writes the dark palette consumed by application adapters, while the local NixVim template writes the Lua table expected by `vim-conf`.

Community templates are disabled. They are fetched and cached at runtime rather than pinned by this flake; applications requiring a community template must be ported into `config/noctalia/templates/` with a reviewed upstream revision first.

## Plugins

The active v5 plugins are vendored in this repository: `cat`, `timer`, `screen_recorder`, `screen_toolkit`, `gamer_mode`, `prismlauncher_instances` and `bitwarden`. Their upstream revisions and local policy are recorded in `plugins/README.md`; automatic updates are disabled, so this repository commit is the review boundary. The Noctalia plugin API is still beta.

The screen recorder and Screen Toolkit use external system binaries, which remain a package concern in `nix-conf`. The Niri bindings route `Mod+Shift+S` to Noctalia’s native `wlr-screencopy` capture and `Mod+Shift+P` to the Screen Toolkit panel; `Mod+G` opens Gamer Mode without adding it to the bar. Prism instances use the FreeSM Flatpak data path and command. Bitwarden is available from the launcher through `/bw` and still requires an interactive CLI login/unlock. Noctalia plugin state is not copied from `Noctara-Dots/local/state`; only stable configuration intent is curated.

## Flake usage

```nix
inputs.noctalia-conf = {
  url = "github:Joaoferraz-byte/noctalia-conf";
  inputs.nixpkgs.follows = "nixpkgs";
};

home-manager.sharedModules = [
  inputs.noctalia-conf.homeModules.default
];
```

The canonical repository URL is `github:Joaoferraz-byte/noctalia-conf`; its source tree contains only Noctalia configuration, reviewed plugins and the Home Manager integration.

## Validation

Run `nix flake check --no-build` in this repository and in `nix-conf`. On hardware, verify one `noctalia` process, the user session target, the generated palette files, plugin discovery, `niri msg windows`, the wallpaper IPC, screen recorder availability and `keyd monitor`. Validate drivers, PipeWire, portals and lock/suspend behavior independently because they are intentionally outside this repository.

## References

[1]: https://docs.noctalia.dev/noctalia/ "Noctalia v5 documentation"
[2]: https://docs.noctalia.dev/noctalia/configuration/ "Noctalia v5 configuration"
[3]: https://docs.noctalia.dev/noctalia/ipc/surfaces/ "Noctalia v5 surface IPC"
[4]: https://docs.noctalia.dev/noctalia/ipc/media-and-ui/ "Noctalia v5 media and UI IPC"
[5]: https://docs.noctalia.dev/noctalia/plugins/ "Noctalia v5 plugins"
[6]: https://docs.noctalia.dev/noctalia/bar/widgets/screenshot/ "Noctalia v5 screenshot widget"
[7]: https://noctalia.dev/plugins/community/screen-toolkit "Noctalia Screen Toolkit plugin"
[8]: https://noctalia.dev/plugins/community/gamer-mode "Noctalia Gamer Mode plugin"
[9]: https://noctalia.dev/plugins/community/prismlauncher-instances "Noctalia Prism Launcher Instances plugin"
[10]: https://noctalia.dev/plugins/official/bitwarden "Noctalia Bitwarden plugin"
